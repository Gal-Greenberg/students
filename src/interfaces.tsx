export interface StudentProps {
    // studentData: {
        id: string
        name: string;
        age: string;
        gender: string;
        school: string;
        city: string;
        gpa: string;
    // }
    // markFunc?: (e: React.ChangeEvent<HTMLInputElement>) => void
}